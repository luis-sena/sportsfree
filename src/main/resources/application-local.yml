logging:
  level:
    root: INFO
    org:
      springframework:
        servlet:
          DispatcherServlet: DEBUG
        web: INFO
    br:
      com:
          sportsfree: DEBUG
spring:
  application:
    name: sportsfree
  datasource:
    username: postgres
    password: postgres
    url: jdbc:postgresql://0.0.0.0:5432/sportsfree

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL95Dialect
    hibernate:
      ddl-auto: update
    show-sql: false

com:
  zaxxer:
    hikari:
      HikariDataSource:
        driverclassname: org.postgresql.Driver

